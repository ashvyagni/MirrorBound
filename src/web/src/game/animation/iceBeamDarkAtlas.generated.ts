// GENERATED by scripts/build_atlas.py -- do not edit by hand.
// Regenerate with: npm run assets

export const ICEBEAMDARK_TEXTURE_KEY = "iceBeamDark" as const;

/** How tall the character itself is inside the frame box, 0..1.
 *  Cells are padded by trails and motion range, so this -- not the frame
 *  height -- is what two characters must be compared on to size them
 *  relative to each other. */
export const ICEBEAMDARK_BODY_RATIO = 0.97287 as const;

/** Every frame shares this source box, so one origin works for all of them. */
export const ICEBEAMDARK_FRAME_SIZE = { width: 762, height: 129 } as const;

/** Origin on the body centre and its vertical centre. */
export const ICEBEAMDARK_ANCHOR = { x: 0.49869, y: 0.49612 } as const;

export const ICEBEAMDARK_FRAMES = {
  cast: ["cast-00", "cast-01"],
  cast_1: ["cast-02", "cast-03"],
  cast_2: ["cast-04", "cast-05"],
  cast_3: ["cast-06", "cast-07"],
} as const;

export type IcebeamdarkClip = keyof typeof ICEBEAMDARK_FRAMES;
