version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: mirrorbound
      POSTGRES_PASSWORD: mirrorbound_password
      POSTGRES_DB: mirrorbound
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
